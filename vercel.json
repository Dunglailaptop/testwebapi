{
  "version": 2,
  "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
  "buildCommand": "npm run build && npx prisma generate",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ]
}